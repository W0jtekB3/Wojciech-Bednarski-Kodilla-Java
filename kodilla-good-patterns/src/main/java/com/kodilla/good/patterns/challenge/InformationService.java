package com.kodilla.good.patterns.challenge;

public class InformationService {
    public void informUser(User user) {
        System.out.println("Sending information to user: " + user.getName());
    }
}